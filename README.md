# DC Maestro UI

Интерактивный UI-прототип системы управления дата-центром (DCIM + WFM) в стиле **DCMAESTRO**.  
Проект предназначен для презентаций, пресейлов и демонстрации того, как может выглядеть рабочее место инженера ЦОД.

---

## 💡 Основная идея

Приложение показывает единый интерфейс для:

- мониторинга ключевых метрик ЦОД;
- визуализации стоек и инфраструктуры;
- управления задачами и сменами;
- будущих модулей: персонал, склад, финансы, проекты, безопасность.

Сейчас это **фронтенд-демо без реального бэкенда** — все данные статичны или сымитированы.

---

## ✨ Реализованные экраны

### 1. Дашборд

- Заголовок: *Обзор ЦОД "Север-1"*  
- KPI-карточки:
  - Uptime (Аптайм)
  - Общая мощность
  - Средняя T° зала
  - Активные заявки
- График «Динамика нагрузки и температуры» за 24 часа (Recharts).
- Блок «Критические алерты» с актуальными событиями.

### 2. Мониторинг (DCIM)

- Сетка стоек (Rack A/B/C).
- Для каждой стойки показаны базовые параметры: нагрузка, мощность и состояние.

### 3. ТО и Ремонты (Maintenance / WFM)

- Таблица текущих задач (заявок).
- Боковая панель смен на сегодня.
- Внизу — AI-подсказка о возможной перегрузке по персоналу.

### 4. Модули-заглушки

Следующие разделы пока отображают состояние **«Модуль в разработке»**:

- Персонал (WFM)
- Склад и Закупки
- Финансы
- Проекты
- Безопасность

Это сделано специально, чтобы показывать дорожную карту продукта, не усложняя код.

---

## 🛠️ Стек технологий

- **React** + **TypeScript**
- **Vite**
- **Tailwind CSS v4** через `@tailwindcss/postcss`
- **Recharts** — графики и визуализация нагрузки/температуры
- **lucide-react** — иконки

---

## 📁 Структура проекта

```bash
dc-maestro/
├── App.tsx              # Корневой layout и роутинг по разделам
├── index.html           # Входная HTML-страница (Vite)
├── index.tsx            # Точка входа React
├── index.css            # Tailwind v4 + базовые стили
├── components/
│   ├── Sidebar.tsx      # Левое навигационное меню
│   ├── Header.tsx       # Шапка с поиском и профилем
│   ├── Dashboard.tsx    # Главный дашборд ЦОД
│   ├── Monitoring.tsx   # Экран инфраструктуры (стойки)
│   └── Maintenance.tsx  # Экран ТО и ремонтов
├── postcss.config.js    # Конфиг PostCSS (Tailwind v4)
├── tailwind.config.js   # (опционален, если используется)
├── tsconfig.json        # Настройки TypeScript
└── vite.config.ts       # Конфиг Vite
🚀 Запуск проекта
bash
Копировать код
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
По умолчанию приложение будет доступно по адресу:

text
Копировать код
http://localhost:3000
⚠️ Ограничения текущей версии
Нет реальной авторизации — пользователь «Алексей Петров, Старший инженер» зашит в интерфейс.

Данные по стойкам, задачам, графикам и алертам захардкожены для демонстрации.

Модули Personnel, Warehouse, Finance, Projects, Security реализованы как заглушки.

Нет интеграции с реальными системами мониторинга, тикетингом или CMDB — только UI.

🔭 Возможные направления развития
Подключение реального API (Prometheus, Zabbix, DCIM-платформы и т.п.).

Настоящая система пользователей и ролей (инженер, дежурный, руководитель смены).

Расширение дашборда: PUE, энергопотребление по зонам, SLA/OLA.

Исторические графики, сценарии аварий, симуляции нагрузок.

Импорт/экспорт конфигурации стоек и оборудования.

📌 Версия
Текущая версия UI-прототипа: v0.2.4.0 LIVE (demo)
Проект собран как «живой макет» для демонстрации интерфейса управления ЦОД без привязки к конкретному вендору.